@model Subject
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<div>
    <div>
        <form method="post" astion="/">
            <label asp-for="kor">국어 : </label>
            <select asp-for="kor" asp-items="Html.GetEnumSelectList<Score>()" class="form-control">
                <option value="">카테고리를 선택해주세요.</option>
            </select>
            <br />
            <label asp-for="eng">영어 : </label>
            <select asp-for="eng" asp-items="Html.GetEnumSelectList<Score>()" class="form-control">
                <option value="">카테고리를 선택해주세요.</option>
            </select>
            <br />
            <label asp-for="math">수학 : </label>
            <select asp-for="math" asp-items="Html.GetEnumSelectList<Score>()" class="form-control">
                <option value="">카테고리를 선택해주세요.</option>
            </select>
            <br />
            <label asp-for="soc">사회 : </label>
            <select asp-for="soc" asp-items="Html.GetEnumSelectList<Score>()" class="form-control">
                <option value="">카테고리를 선택해주세요.</option>
            </select>
            <br />
            <label asp-for="sci">과학 : </label>
            <select asp-for="sci" asp-items="Html.GetEnumSelectList<Score>()" class="form-control">
                <option value="">카테고리를 선택해주세요.</option>
            </select>
            <br />
            <input type="submit" value="submit" />
        </form>
    </div>
</div>